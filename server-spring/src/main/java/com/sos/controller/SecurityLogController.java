package com.sos.controller;

/**
 * Security log endpoints have been merged into UserController
 * (GET /api/users/security-logs) to match the Node.js route structure.
 *
 * This class is intentionally empty — kept only to avoid breaking the build
 * if anything still references it by classname.
 */
public class SecurityLogController {
    // Intentionally empty — see UserController
}
